import 'package:flutter/material.dart';

class AddDiagnosisScreen extends StatelessWidget {
  const AddDiagnosisScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Add Diagnosis Screen')));
  }
}