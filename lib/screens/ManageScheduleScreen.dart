import 'package:flutter/material.dart';


class ManageScheduleScreen extends StatelessWidget {
  const ManageScheduleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Manage Schedule Screen')));
  }
}