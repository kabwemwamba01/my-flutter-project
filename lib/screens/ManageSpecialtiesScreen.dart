
import 'package:flutter/material.dart';

class ManageSpecialtiesScreen extends StatelessWidget {
  const ManageSpecialtiesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Manage Specialties Screen')));
  }
}