import 'package:flutter/material.dart';

class EditClinicInfoScreen extends StatelessWidget {
  const EditClinicInfoScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Edit Clinic Info Screen')));
  }
}