import 'package:flutter/material.dart';

class SearchClinicDoctorScreen extends StatelessWidget {
  const SearchClinicDoctorScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Search Clinic & Doctor Screen')));
  }
}